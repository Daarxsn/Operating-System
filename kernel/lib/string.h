#ifndef XYRIS_LIB_STRING_H
#define XYRIS_LIB_STRING_H

/*
 * The kernel has one canonical string API in kernel/include/string.h.
 * Keep this compatibility header for sources that include
 * ../lib/string.h so declarations cannot drift between copies.
 */
#include "../include/string.h"

#endif
